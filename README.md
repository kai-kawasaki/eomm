How to use program:

Run program using Cmake
Click either "Run Benchmarks" or "Show performance graph"
Look at command line for live updates on sorts being ran
Click show graph if bar charts are not displayed already
Run benchmark reruns tests
Show performance graph will run the benchmarks only if it has not been run yet.
